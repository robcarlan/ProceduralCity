#pragma once

enum class geogType {
	LAND,
	WATER,
	PARK
};

enum class roadType {
	STREET,
	MAINROAD
};

enum class roadPattern {
	NOPATTERN,
	MANHATTAN,
	RADIAL,
	SANFRAN
};

enum buildingStyle {
	RESIDENTIAL,
	COMMERCIAL,
	FINANCIAL,
	SKYSCRAPER
};